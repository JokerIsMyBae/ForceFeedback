#include <Arduino.h>

#define ENA 2
#define In1 3
#define In2 4


void setup() {
  pinMode(ENA, OUTPUT);
  pinMode(In1, OUTPUT);
  pinMode(In2, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:
}